imports:
    - { resource: config.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        firephp:
            type:  firephp
            level: info

assetic:
    use_controller: true

fos_facebook:
    alias:  facebook
    app_id: 554560297916061
    secret: bfb545cda70b4fb92dc5cbfeb39352b9
    cookie: true
    permissions: [email]

#swiftmailer:
#    delivery_address: me@example.com
