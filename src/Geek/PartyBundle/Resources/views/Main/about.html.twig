{% extends 'GeekPartyBundle::parent.html.twig' %}

{% block main %}

    <div class="row main-text">
        <div class="col-md-12">
            {% if text.body is not null %}
                {{ text.body|raw }}
            {% endif %}
        </div>
    </div>
    
{% endblock %}
