<h1>WorkAuthor creation</h1>

<form action="{{ path('admin_workauthor_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_workauthor') }}">
            Back to the list
        </a>
    </li>
</ul>
